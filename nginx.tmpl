# generated by confd {{datetime}}
{{range gets "/maestro.io/apps/*"}}
{{$upstream := json .Value}}
# nginx vhost: {{base .Key}}.maestro.io
upstream {{base .Key}} {
{{$skydnsdir := printf "%s/*" $upstream.watchdir}}{{range gets $skydnsdir}}
  server {{$skydnsrr := json .Value}}{{$skydnsrr.host}}:{{$upstream.port}};{{end}}
}

server {
    listen 0.0.0.0:80;
    server_name {{base .Key}}.maestro.io;

    access_log /var/log/nginx/{{base .Key}}-access.log;
    error_log /var/log/nginx/{{base .Key}}-error.log;

    location / {
                proxy_pass http://{{base .Key}};
                proxy_http_version 1.1;
                proxy_set_header Connection "";
                proxy_redirect     off;
                proxy_set_header   Host $host;
                proxy_set_header   X-Real-IP $remote_addr;
                proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header   X-Forwarded-Host $server_name;
    }
}
{{end}}
